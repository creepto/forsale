{
  "name": "forsale",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com-creepto:creepto/forsale.git",
  "author": "Guy Vider <guy@guyvider.com>",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf build",
    "build": "parcel build --public-url ./ --no-source-maps -d build index.html",
    "predev": "rm -rf build",
    "dev": "parcel --no-source-maps -d build index.html"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-interpolate-html": "^1.0.1"
  }
}
